# 模块参数与接线

本文集中维护 Minibox 的实际模块接线。修改硬件时同步更新本文和固件配置，
避免文档与接线不一致。

## 主控：ESP32-S3 N16R8

| 参数 | 值 |
|---|---|
| 芯片 | ESP32-S3，实板识别 revision v0.2 |
| Flash | 16 MB，Quad SPI，3.3 V |
| PSRAM | 8 MB，Octal，3.3 V |
| 固件 Flash 配置 | DIO，80 MHz |
| 固件 PSRAM 配置 | Octal，40 MHz |
| 当前电脑串口 | **COM8**，USB-Enhanced-SERIAL CH343 |
| 串口日志 | UART0，115200 波特率 |
| 固件项目 | [`esp32_idf_s3n16r8`](../esp32_idf_s3n16r8/README.md) |

COM8 是当前 Windows 分配的端口，更换电脑或 USB 接口后可能变化。
GPIO19/20 保留给原生 USB；不使用 GPIO35/36/37，以避免与 N16R8
Octal PSRAM 冲突。

## 屏幕模块：ST7796 SPI

| 参数 | 值 |
|---|---|
| 控制器 | ST7796 |
| 显示分辨率 | **480 × 320**，横屏 |
| 显示朝向 | 横屏交换坐标轴（MV），清除 MX/MY 镜像；BGR 开启时 MADCTL=0x28 |
| 接口 | SPI，模式 0，SPI2_HOST |
| 默认 SPI 时钟 | 40 MHz，可配置为 1–40 MHz |
| 屏幕驱动 | 乐鑫 `espressif/esp_lcd_st7796` 1.4.0，纯 ESP-IDF |
| 像素格式 | RGB565，16 bit，高字节先发送 |
| 字体 | 24 px 中英文抗锯齿字体，存放在 SPIFFS，见[字体说明](display-fonts.md) |
| 默认色序 | BGR，可配置 |
| 默认反色 | 关闭，可配置 |
| 背光控制 | 默认高电平点亮，可配置极性 |
| 背光调制 | 无 PWM；启动点亮后保持恒定电平 |

| 屏幕引脚标识 | ESP32-S3 引脚 | 作用 |
|---|---|---|
| LED / BL | **GPIO9** | 背光控制 |
| SCK / CLK | **GPIO10** | SPI 时钟 |
| SDI / MOSI | **GPIO11** | 主控向屏幕发送数据 |
| DC / RS | **GPIO12** | 命令 / 数据选择 |
| Reset / RST | **GPIO13** | 硬件复位，低电平有效 |
| CS | **GPIO14** | 片选，低电平有效 |
| SDO / MISO（若有） | 不接 | 当前固件只写屏幕，不读取 ID |
| GND | GND | 与主控共地 |
| VCC | 按具体模块规格 | 额定供电电压尚未确认，不能仅凭 ST7796 型号推断 |

屏幕逻辑信号必须兼容 **3.3 V**。GPIO9 只能连接背光控制输入：
如果模块 LED 引脚是裸背光供电端，需采用额定限流和晶体管/MOSFET 驱动，
不能直接由 GPIO 承担背光电流。

屏幕接线常量位于
[`main/app_config.h`](../esp32_idf_s3n16r8/main/app_config.h)。
SPI 时钟、色序、反色和背光极性位于
`idf.py menuconfig` → `Minibox hardware test`。

## 摇杆模块：XYK

X/Y 是模拟信号，表示摇杆在两个方向的偏移量；K 是数字按键信号。
最初记录为低电平有效，后续实物反馈按下/松开状态相反，因此当前固件按
**高电平为按下**处理，可在菜单切换。串口同时输出 `K_LEVEL` 原始电平与
`PRESSED` 判定状态，便于核对实际模块，而不是将 UI 状态当作电气规格。

| 摇杆信号 | ESP32-S3 接线 | 类型 / 行为 |
|---|---|---|
| X | **GPIO4 / ADC1_CH3** | 模拟输入，左右偏移量 |
| Y | **GPIO5 / ADC1_CH4** | 模拟输入，上下偏移量 |
| K | **GPIO6** | 数字输入，保留内部上拉；当前按实物反馈采用高有效，可配置 |
| VCC / + | **3.3 V** | 避免模拟输出超过主控允许电压 |
| GND | GND | 与主控、屏幕共地 |

不要给摇杆接 5 V 后直接将 X/Y 输出接入 ESP32-S3。
X/Y 不产生按键事件；只有 K 稳定进入所配置的有效电平才计为一次按下。

| 固件参数 | 默认值 |
|---|---|
| ADC | 12 bit，0–4095，12 dB 衰减 |
| 输入采样周期 | 10 ms，每轴取 8 次均值 |
| 中心校准 | 启动提示后等待 1.5 秒，采样约 640 ms；保持摇杆居中 |
| 归一化偏移 | −100% 到 100%，以实测中心分别缩放两侧范围 |
| 屏幕 X/Y 精度 | −1.00 到 1.00，两位小数；原始 ADC 仅输出到串口 |
| 显示稳定处理 | 偏移达到 0.05 才离开中心、回到 0.02 内归零；变化不超过 0.01 保持原值 |
| 方向进入 / 释放阈值 | 25% / 15% |
| K 去抖 | 稳定 30 ms |
| 长按 K | 不重复增加计数 |
| 方向反转 | 默认 X 反转、Y 不反转，按实物反馈设置 |

当前 X 数值较大对应左、较小对应右；Y 较小对应上、较大对应下。
安装方向相反时，在
`Minibox hardware test` 菜单中反转对应轴，不必交换接线。

## 板载 RGB LED

| 参数 | 值 |
|---|---|
| 控制引脚 | **GPIO48** |
| 控制方式 | WS2812 单线时序，RMT 输出 |
| 固件行为 | 启动时发送全零 RGB 数据，将板载灯熄灭，随后保持引脚低电平 |

仅拉低 GPIO 不会清除 WS2812 已锁存的颜色，因此固件会先发送黑色数据。
GPIO48 与屏幕背光 GPIO9 独立；关闭板载灯不影响屏幕。
板上若还有独立电源指示灯，则不能通过 GPIO48 关闭。

## 接线检查与上电

1. 断电检查 GPIO9–14 的屏幕信号顺序，确认 LED 是控制端还是供电端。
2. 检查 X=GPIO4、Y=GPIO5、K=GPIO6，所有模块共地，摇杆使用 3.3 V。
3. 连接开发板 USB-UART 接口，在当前电脑使用 COM8 烧录。
4. 松开摇杆，等待中心校准完成；检查色条、边框、摇杆光标及 K 按键计数。

完整的构建、烧录及测试操作见
[`固件说明`](../esp32_idf_s3n16r8/README.md)；
模块机械尺寸及外壳安装参数见
[`模型尺寸说明`](../models/module-dimensions.md)。
